<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>Statistiques des Logos</title>
    <style>
        * {
            box-sizing: border-box;
        }
        body {
            margin: 0;
            padding: 32px;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
            color: #0f172a;
            background: #f8fafc;
        }
        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 16px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #ffffff;
            padding: 32px;
            text-align: center;
        }
        .title {
            font-size: 32px;
            font-weight: 700;
            margin: 0 0 8px 0;
            letter-spacing: -0.5px;
        }
        .subtitle {
            font-size: 15px;
            color: #cbd5e1;
            margin: 0;
        }
        .content {
            padding: 24px;
        }
        .stats-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            margin-top: 0;
        }
        .stats-table thead th {
            padding: 14px 12px;
            text-align: left;
            font-weight: 700;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border-bottom: 2px solid #e2e8f0;
            background: #f8fafc;
        }
        .stats-table thead th:first-child {
            border-top-left-radius: 8px;
        }
        .stats-table thead th:last-child {
            border-top-right-radius: 8px;
        }
        .stats-table thead th.center {
            text-align: center;
        }
        .stats-table tbody td {
            padding: 12px;
            font-size: 12px;
            border-bottom: 1px solid #f1f5f9;
        }
        .stats-table tbody tr:last-child td {
            border-bottom: none;
        }
        .stats-table tbody tr:hover {
            background: #f9fafb;
        }
        .stats-table td.center {
            text-align: center;
        }
        .logo-cell {
            vertical-align: middle;
        }
        .logo-img {
            display: inline-block;
            width: 40px;
            height: 40px;
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            padding: 4px;
            background: #ffffff;
            vertical-align: middle;
            margin-right: 12px;
            text-align: center;
        }
        .logo-img img {
            width: 32px;
            height: 32px;
            vertical-align: middle;
        }
        .logo-name {
            font-weight: 600;
            color: #1e293b;
        }
        .tier-badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 6px;
            font-weight: 700;
            font-size: 13px;
            min-width: 40px;
            text-align: center;
        }
        .tier-s { background: #f87171; color: #ffffff; }
        .tier-a { background: #fb923c; color: #ffffff; }
        .tier-b { background: #facc15; color: #78350f; }
        .tier-c { background: #4ade80; color: #ffffff; }
        .tier-d { background: #60a5fa; color: #ffffff; }
        .no-data {
            text-align: center;
            padding: 64px 32px;
            color: #64748b;
            font-style: italic;
            font-size: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="title">📊 Statistiques des Logos</h1>
            <p class="subtitle">Nombre de placements par catégorie · Généré le {{ date }}</p>
        </div>
        <div class="content">

            {% if statistics is empty %}
                <div class="no-data">
                    Aucune donnée de placement disponible pour le moment.
                </div>
            {% else %}
                <table class="stats-table">
                    <thead>
                        <tr>
                            <th style="width: 40%;">Logo</th>
                            <th class="center" style="width: 12%;">S</th>
                            <th class="center" style="width: 12%;">A</th>
                            <th class="center" style="width: 12%;">B</th>
                            <th class="center" style="width: 12%;">C</th>
                            <th class="center" style="width: 12%;">D</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for stat in statistics %}
                            <tr>
                                <td class="logo-cell">
                                    <div class="logo-img">
                                        <img src="{{ stat.imageUrl }}" alt="{{ stat.name }}" />
                                    </div>
                                    <span class="logo-name">{{ stat.name }}</span>
                                </td>
                                <td class="center"><span class="tier-badge tier-s">{{ stat.S }}</span></td>
                                <td class="center"><span class="tier-badge tier-a">{{ stat.A }}</span></td>
                                <td class="center"><span class="tier-badge tier-b">{{ stat.B }}</span></td>
                                <td class="center"><span class="tier-badge tier-c">{{ stat.C }}</span></td>
                                <td class="center"><span class="tier-badge tier-d">{{ stat.D }}</span></td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            {% endif %}
        </div>
    </div>
</body>
</html>
