# Tests & Qualite

## Etat actuel

- Aucun dossier `api/tests` et aucune dependance de tests (ex. PHPUnit) dans `api/composer.json`.
- Aucun script de test defini cote backend.
- Cote frontend, un script `npm run lint` est disponible (ESLint, `ui/package.json`).
- Les validations actuelles sont faites via tests manuels (UI + API).

## Outils presents

- ESLint via Next.js (script `lint`).
- Pas d'outil de formatage ou d'analyse statique PHP declare dans `api/composer.json`.
